import React from 'react';

export interface CustomRoutesProps {
  noLayout?: boolean;
  children: React.ReactElement;
}

export const CustomRoutes: React.FC = React.memo(() => {
  // TODO
  return null;
});
CustomRoutes.displayName = 'CustomRoutes';
